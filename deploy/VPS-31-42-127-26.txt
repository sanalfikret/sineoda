Plooy VPS — 31.42.127.26 (Fikret sunucusu)
============================================

DOĞRU KLASÖR (PuTTY):
  cd /root/plooy

YANLIŞ (bu sunucuda YOK):
  /opt/Plooy
  /opt/plooy
  /opt/sineoda

NGINX → Docker:
  http://127.0.0.1:3001  (port 80 dışarıdan)

VERİ:
  /root/plooy/persistent/data/sineoda.db

TEK KOMUT DEPLOY:
  cd /root/plooy && git fetch origin main && git reset --hard origin/main && bash deploy/deploy-vps.sh

DOĞRULAMA:
  curl -s http://127.0.0.1:3001/api/health | grep gitSha
  curl -s http://127.0.0.1:3001/ | grep -o 'assets/index-[^"]*\.js'

Tarayıcı: Ctrl+Shift+R → http://31.42.127.26/
