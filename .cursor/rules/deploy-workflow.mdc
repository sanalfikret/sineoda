---
description: Merge, push, and verify deploy after completing feature work
alwaysApply: true
---

# Deploy workflow (Plooy)

## Tek kaynak: VPS (kiralık sunucu)

Site + API + DB tek makinede. Render/Vercel/GitHub **runtime'da kullanılmaz**.

Detay: `deploy/PRODUCTION-VPS.txt`

## Veri kalıcılığı (kritik)

Canlı veri **git'te değil**, VPS'te `persistent/data/Plooy.db` dosyasında.

- Deploy sadece kodu günceller; `persistent/` klasörüne dokunmaz.
- **Asla** `docker-compose down -v` veya `rm -rf persistent/` çalıştırma.
- Her deploy öncesi `deploy/backup-vps.sh` otomatik çalışır.
- Deploy sonrası `/api/health` → `storage.userCount` düşmemeli.

## Push öncesi (zorunlu)

```bash
npm run check:deploy
```

## Deploy — GitHub + VPS (önerilen)

### PC'de (kod değişince)
```bash
git add .
git commit -m "açıklama"
git push origin main
```

GitHub şifresi kabul etmez — **Personal Access Token** kullan (Settings → Developer settings → Tokens).

### VPS'te (PuTTY)
```bash
cd /opt/Plooy
git pull
bash deploy/rebuild-vps.sh
```

`rebuild-vps.sh` yoksa: `bash deploy/rebuild-vps.sh` veya kökte `bash rebuild-vps.sh`

### Yedek: GitHub'sız aktarım

```powershell
npm run deploy:vps
```

## .env (VPS)

- `PERSIST_DIR=/opt/Plooy/persistent`
- `VITE_API_URL=` boş (same-origin)
- `JWT_SECRET` deploy sonrası değiştirme

## Verify deploy

- `curl -s http://127.0.0.1:3001/api/health` → `dbExists: true`, `jwtExpiresIn: "30d"`
- Tarayıcı hard-refresh (`Ctrl+Shift+R`)
