# Frontend (Vercel)
# Yerel geliştirme: boş bırak (vite proxy → localhost:3001). Render yalnızca Vercel statik host için.
# VPS / Docker: kesinlikle boş — deploy/update-vps.sh build sırasında zorlar.
VITE_API_URL=
